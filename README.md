# BeeSolve packages

## Publishing

When publishing you need to use `bun publish` which will automatically resolves `workspace:*` and `catalog:` placeholders.

```bash
bun publish --access public
```
