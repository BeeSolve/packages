# Node.js Lambda Fetch API

This package contains functions which helps you to run any Fetch API workload within AWS Node.js Lambda.

# Installation

```bash
npm i @beesolve/lambda-fetch-api
```

# Usage

todo

# Roadmap

- [ ] split HTTP v1/v2 functionality - be more explicit
- [ ] add `valibot` validations of events instead of relying on `@types/aws-lambda` package
- [ ] provide versions for authorized requests (API Gateway and HTTP Gateway)
- [ ] provide repsonse stream versions
